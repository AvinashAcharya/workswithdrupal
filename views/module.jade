extends layout
block content

  if works
    p
      | Yes!
      | <strong><a href="https://drupal.org/project/#{module}">#{name}</a></strong>
      | works with <strong>Drupal #{version}</strong>.
  else
    p
      | No,
      | <strong><a href="https://drupal.org/project/#{module}">#{name}</a></strong>
      | does not work with <strong>Drupal #{version}</strong>.

  p
    a(href='/') Check another.
